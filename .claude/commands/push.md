---
description: "Push current branch to origin (ChristopherSNelson/bcherny-claude)"
---

1. Run `git status` to confirm there are no uncommitted changes. If there are, stop and tell the user to commit first.
2. Run `git push origin HEAD` to push the current branch to origin.
3. Report the branch and remote URL that were pushed to.
